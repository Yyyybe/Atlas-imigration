import { PlaceholderPage } from "@/app/components/PlaceholderPage";

export default function DocumentsPage() {
  return (
    <PlaceholderPage
      title="Documents"
      body="Required files, upload status, and apostille checks will appear here. Two items currently need attention, matching the badge in navigation."
    />
  );
}
