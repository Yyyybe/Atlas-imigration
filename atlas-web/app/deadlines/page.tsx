import { PlaceholderPage } from "@/app/components/PlaceholderPage";

export default function DeadlinesPage() {
  return (
    <PlaceholderPage
      title="Deadlines"
      body="A calendar of submissions and appointments will expand this view. The Overview already lists the next two dates so nothing urgent is hidden."
    />
  );
}
