import { PlaceholderPage } from "@/app/components/PlaceholderPage";

export default function JourneyPage() {
  return (
    <PlaceholderPage
      title="Journey"
      body="Your staged path from diagnosis to permit will live here. Atlas still uses this shell so the Overview remains the place that answers “what should I do now?”"
    />
  );
}
